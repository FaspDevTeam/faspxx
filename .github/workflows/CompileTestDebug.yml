name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make directory
      run: mkdir cmake_debug
    - name: cmake
      run: cd cmake_debug && cmake ..
    - name: make
      run: make
